import { css } from '@emotion/react'
import { dialogDefaultCss } from '../dialogs'

export const menuDialogCss = css`
  ${dialogDefaultCss};

  h2 {
    margin: 0 0 0.5em;
  }

  p {
    margin: 0.5em 0;
  }
`