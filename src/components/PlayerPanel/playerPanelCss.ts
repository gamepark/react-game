import { css } from '@emotion/react'

/**
 * Shared style of the badges displayed on the right side of the player panel (timer & counters).
 */
export const rightAlignment = css`
  left: initial;
  right: 0.2em;
  font-size: 2.5em;
`
